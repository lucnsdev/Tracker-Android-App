<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="48"
        android:startColor="@color/incoming_start"
        android:centerColor="@color/incoming_center"
        android:endColor="@color/incoming_end"
        android:type="linear" />
</shape>